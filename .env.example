# Required. Server-side only -- read in src/lib/anthropic.ts and never sent to the browser.
ANTHROPIC_API_KEY=

# Optional. Defaults to "claude-sonnet-5" if unset. Swap to a higher-tier model
# (e.g. "claude-opus-5") for a live demo without touching any code.
ANTHROPIC_MODEL=
